@@ -3,2 +3,3 @@
 
+if [ -z "$PATH" ]; then
 if [ "$(id -u)" -eq 0 ]; then
@@ -8,2 +9,3 @@
 fi
+fi
 export PATH
@@ -9,4 +9,6 @@
 export PATH
 
+ETC_PROFILE=washere
+
 if [ "${PS1-}" ]; then
   if [ "${BASH-}" ] && [ "$BASH" != "/bin/sh" ]; then
